const Tambah = {
  async init() {
    await this._dataAwal();
    this._initialHandler();
  },

  async _dataAwal() {
    // console.log("init handler Tambah");
  },

  _initialHandler() {
    // console.log("init handler Tambah");
  },
};

export default Tambah;
