const Pengaturan = {
  async init() {
    await this._dataAwal();
    this._initialHandler();
  },

  async _dataAwal() {
    // console.log("init Data Pengaturan");
  },

  _initialHandler() {
    // console.log("init handler Pengaturan");
  },
};

export default Pengaturan;
